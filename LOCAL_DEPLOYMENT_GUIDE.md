# AI交易系统本地部署指南

## 📋 目录

- [系统要求](#系统要求)
- [快速部署](#快速部署)
- [详细步骤](#详细步骤)
- [常见问题](#常见问题)
- [功能验证](#功能验证)
- [后续配置](#后续配置)

## 🔧 系统要求

### 操作系统支持
- ✅ Windows 10/11 (64位)
- ✅ macOS 10.15+ (64位)
- ✅ Linux (Ubuntu 18.04+, CentOS 7+)

### 必需软件

#### 后端环境
- **Go 1.25+** - Go编程语言环境
  - 下载地址: https://golang.org/dl/
  - 安装后验证: `go version`

#### 前端环境
- **Node.js 18+** - JavaScript运行环境
  - 下载地址: https://nodejs.org/
  - 安装后验证: `node --version`
  - 包管理器: `npm` (随Node.js安装)

### 可选工具
- **Git** - 版本控制工具
- **VS Code** - 代码编辑器
- **Postman** - API测试工具

## 🚀 快速部署

### Windows用户

```cmd
# 1. 进入项目目录
cd E:\AI交易系统\AIZH

# 2. 运行部署脚本
deploy.bat
```

### Linux/macOS用户

```bash
# 1. 进入项目目录
cd /path/to/AIZH

# 2. 给脚本执行权限
chmod +x deploy.sh

# 3. 运行部署脚本
./deploy.sh
```

## 📝 详细步骤

### 第一步：环境检查

#### 检查Go环境
```bash
# 检查Go版本
go version

# 应该显示类似：go version go1.25.0 windows/amd64
```

#### 检查Node.js环境
```bash
# 检查Node.js版本
node --version

# 检查npm版本
npm --version

# 应该显示类似：v18.17.0
```

### 第二步：项目准备

```bash
# 进入项目根目录（包含go.mod的目录）
cd AIZH

# 确认项目结构
ls -la
# 应该看到：go.mod, web/, api/, config/ 等
```

### 第三步：安装后端依赖

```bash
# 下载Go模块依赖
go mod download

# 验证依赖
go mod verify
```

### 第四步：安装前端依赖

```bash
# 进入前端目录
cd web

# 安装npm包
npm install

# 返回根目录
cd ..
```

### 第五步：配置文件设置

#### 创建环境变量文件 (.env)
```bash
# 创建.env文件
cat > .env << EOF
# AI交易系统环境变量
APP_VERSION=1.0.0
BUILD_TIME=$(date '+%Y-%m-%d %H:%M:%S')
JWT_SECRET=your-jwt-secret-key-change-in-production
BETA_MODE=false
API_PORT=8080

# 数据库配置
DB_PATH=./data/nofx.db

# 交易所API配置（可选，先留空）
BINANCE_API_KEY=
BINANCE_SECRET_KEY=

# AI模型配置（可选，先留空）
DEEPSEEK_API_KEY=
QWEN_API_KEY=
EOF
```

#### 创建配置文件 (config.json)
```bash
cat > config.json << EOF
{
  "api_server_port": 8080,
  "beta_mode": false,
  "jwt_secret": "your-jwt-secret-key-change-in-production",
  "auto_update_enabled": true,
  "version_check_interval": "1h"
}
EOF
```

#### 创建必要目录
```bash
# 创建数据目录
mkdir -p data logs
```

### 第六步：编译后端

```bash
# 编译Go程序
go build -o nofx .

# 验证编译结果
ls -la nofx*
# 应该看到：nofx (Linux/macOS) 或 nofx.exe (Windows)
```

### 第七步：启动服务

#### 启动后端服务
```bash
# 方式1：直接运行（前台）
./nofx

# 方式2：后台运行（Linux/macOS）
nohup ./nofx > logs/backend.log 2>&1 &

# 方式3：使用脚本
./deploy.sh  # 自动启动所有服务
```

#### 启动前端服务
```bash
# 进入前端目录
cd web

# 启动开发服务器
npm run dev

# 或者使用脚本自动启动
cd .. && ./deploy.sh
```

### 第八步：验证部署

#### 检查服务状态
```bash
# 检查后端API
curl http://localhost:8080/api/health

# 检查前端
curl http://localhost:3000
```

#### 访问Web界面
- **主界面**: http://localhost:3000
- **API文档**: http://localhost:8080/api/health
- **版本管理**: http://localhost:3000/version

## 🎯 功能验证

### 1. 基础功能验证

#### API健康检查
```bash
curl http://localhost:8080/api/health
# 应返回: {"status":"ok","time":"..."}
```

#### 获取当前版本
```bash
curl http://localhost:8080/api/version/current
# 应返回当前版本信息
```

### 2. 用户注册和登录

1. 访问 http://localhost:3000
2. 点击"注册"
3. 输入邮箱和密码
4. 使用Google Authenticator扫描二维码
5. 输入验证码完成注册
6. 使用邮箱密码登录，然后输入2FA验证码

### 3. 版本管理功能

1. 登录后访问"版本更新"页面
2. 点击"检查更新"按钮
3. 查看更新状态和历史记录
4. 测试自动更新设置

### 4. AI交易员管理

1. 配置AI模型（在AI模型页面）
2. 配置交易所（在交易所页面）
3. 创建AI交易员
4. 启动交易员并监控状态

## ⚙️ 配置说明

### 重要配置项

#### 1. JWT密钥
```bash
# 生成强随机密钥
openssl rand -hex 32

# 更新.env文件中的JWT_SECRET
JWT_SECRET=your-generated-secret-key
```

#### 2. 交易所API配置
```env
# 在.env文件中配置
BINANCE_API_KEY=your_binance_api_key
BINANCE_SECRET_KEY=your_binance_secret_key

# 或通过Web界面配置
```

#### 3. AI模型API配置
```env
# DeepSeek API
DEEPSEEK_API_KEY=your_deepseek_api_key

# 通义千问API
QWEN_API_KEY=your_qwen_api_key
```

### 端口配置

如果需要修改端口：

#### 后端API端口（默认8080）
```env
# .env文件
API_PORT=8080

# config.json文件
{
  "api_server_port": 8080
}
```

#### 前端端口（默认3000）
```bash
# web/vite.config.ts
export default defineConfig({
  server: {
    port: 3000,
    // ...
  }
})
```

## 🐛 常见问题

### 问题1：Go版本过低
**错误**: `go version go1.20 linux/amd64`

**解决**:
```bash
# 下载并安装Go 1.25+
# 访问: https://golang.org/dl/
```

### 问题2：Node.js版本过低
**错误**: `node v14.17.0`

**解决**:
```bash
# 使用nvm管理Node.js版本
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 18
nvm use 18
```

### 问题3：端口被占用
**错误**: `listen tcp :8080: bind: address already in use`

**解决**:
```bash
# 查找占用端口的进程
# Linux/macOS
lsof -i :8080
netstat -tlnp | grep :8080

# Windows
netstat -ano | findstr :8080

# 停止进程
kill -9 <PID>
# 或修改配置使用其他端口
```

### 问题4：依赖下载失败
**错误**: `network unreachable` 或 `timeout`

**解决**:
```bash
# 设置Go代理（中国用户）
export GOPROXY=https://goproxy.cn,direct

# 设置npm镜像（中国用户）
npm config set registry https://registry.npmmirror.com
```

### 问题5：前端编译错误
**错误**: TypeScript编译错误

**解决**:
```bash
# 清理缓存
cd web
rm -rf node_modules package-lock.json
npm install

# 检查TypeScript配置
npx tsc --noEmit
```

### 问题6：数据库文件权限问题
**错误**: `permission denied` 数据库文件

**解决**:
```bash
# 检查数据目录权限
ls -la data/

# 修复权限
chmod 755 data/
chmod 644 data/nofx.db
```

### 问题7：2FA验证问题
**问题**: 无法扫描二维码或验证码无效

**解决**:
1. 确保手机时间准确
2. 尝试其他2FA应用（如Authy）
3. 重新生成二维码
4. 检查系统时间是否同步

## 🔧 服务管理

### 启动服务
```bash
# 自动启动所有服务
./deploy.sh

# 手动启动
./nofx &                    # 后端
cd web && npm run dev &      # 前端
```

### 停止服务
```bash
# 自动停止所有服务
./stop.sh

# 手动停止
pkill -f nofx               # 停止后端
pkill -f "npm run dev"       # 停止前端
```

### 重启服务
```bash
./stop.sh && ./deploy.sh
```

### 查看日志
```bash
# 后端日志
tail -f logs/backend.log

# 前端日志
tail -f logs/frontend.log

# 实时日志
tail -f logs/*.log
```

## 📊 性能优化

### 后端优化
- 使用生产模式编译：`go build -ldflags "-s -w"`
- 配置适当的数据库连接池
- 启用Gzip压缩

### 前端优化
- 生产构建：`npm run build`
- 配置CDN加速
- 启用代码分割

## 🔒 安全配置

### 生产环境安全
1. 更改默认JWT密钥
2. 启用HTTPS
3. 配置防火墙
4. 定期备份数据
5. 监控系统日志

### API安全
- 限制API调用频率
- 输入验证和过滤
- SQL注入防护
- XSS攻击防护

## 📞 技术支持

如遇到部署问题，请提供以下信息：

1. **系统信息**：操作系统、Go版本、Node.js版本
2. **错误日志**：完整的错误信息
3. **配置文件**：.env和config.json（隐去敏感信息）
4. **网络环境**：是否有代理或防火墙限制

### 获取帮助
- 📧 邮箱: support@nofx.com
- 🐛 问题反馈: GitHub Issues
- 📖 文档: 项目Wiki

---

**注意**: 这是本地开发环境部署指南。生产环境部署请参考生产部署文档。